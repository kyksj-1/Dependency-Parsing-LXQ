---
project: EntroDiff 模型 NeurIPS 2026 代码开发+论文写作
---

## 角色扮演

你选择NLP领域最权威的一位论文写手进行扮演（要求，该任务真实存在并说明选择的理由）

---

## 要求

- 所有文档类输出，如无特殊要求，均以 markdown 格式输出到项目根目录的 `Docs` 文件夹（已建好，归位规则见下文工作区约定；**默认 `Docs`**）*
- 当你有疑问的时候，可以向我询问进行确认（如 `AskUserQuestion` 工具）。当你有 90% 以上的把握理解我的意图时再开始行动
- 服务器配置在SERVER.md文件中 
-  `D:\发送给别人\lxq\docs` 里面装有用于论文的一些结果
- 服务器上任何已有的进程都不能清除！先问我意见
- 一切代码 要在PC上同步。PC相当于是最后一个代码备份和分发的防线

---
## 总体信息（持续性）

### 投稿目标 · 项目代号

- **作业**：以准发表的水平，用NeurIPS的模板完成一次作业报告
- **项目代号**：（未定，由你取名）
- **方向**：NLP/LM
- **路径**：（文档讨论，由你补充）
- **风格**：专业程度NeurIPS 偏好——写出水准；语言风格，由于是作业，可以轻松一点
### 论文写法（持续性 · 写作时必须对齐 · 暂定 · 可持续更新）

- **introduction**：介绍该任务
- **our work**：介绍我们完成了什么
- **methodology**：介绍我们用的方法（每个可作为一个section）：
	- 结构
	- embedding
	- layer
	- attention（这是layer中的一部分，但是也可以单独拿出来讲）
	- 优化器
- **theory**：对问题进行数学建模；分析背后的理论、所用到的数学公式。**所有公式一定要denote清楚！**
- **experiment**：介绍我们做了的实验（每个实验一个section）：
	- 【具体做了什么实验，见 `D:\发送给别人\lxq\docs` 中内容。注意，01文档不是本项目内容】
	- 重点细节需点明，对比要突出，讲解要清晰
- **conclusion**：简要说明我们的结论。要简要，对每个的结论和讨论应该 `experiment` 章节完成，这里只是点一下
- **特别注意**：
	- 必要处点出我们的**motivation**
	- **减少AI味道**，具体体现见后文
	- 详略得当，轻重分明，不要在某个小点大谈特谈、其它模块一笔带过

## 工作区目录约定

（如有需要由你补充）

## NeurIPS 2026 排版规范（强制）


- **主控**：`Paper/neurips_2026.tex`，样式 `Paperneurips_2026.sty`
- **作者**：由于是拟投稿，所以可以写上名字（共同一作）`kyksj-1  Shanghai Jiao Tong University  kyksj1225@gmail.com` & `Suky  Wuhan University 2024302182005@whu.edu.cn`
- **样式**：分成多个小节，`\input` 插入
- **公式**：必须用 `\begin{equation}` / `\begin{align}`，**严禁** `$$...$$`
- **表格**：`booktabs` 宏包，**严禁**垂直分割线
- **图表 caption**：必须包含 *Key take-away message*；按论文惯例，table caption 放在表格上方，figure caption 放在图片下方。
- **引用**：`natbib`，文内统一 `\citet{}`（叙述式）/ `\citep{}`（括号式）【引用须是真实的参考文献。由于不是真的会议文稿，我们不需要水citation的数量。确保真的是关键的就行了】
- **源码可读性**：论文 `.tex` 的普通正文必须“一段一行”，不要把同一自然段硬换成多行；公式、表格、算法、列表、宏定义等结构化 LaTeX 环境按语义保留必要换行。
- **用户批注处理**：论文片段中若出现 `\ljz{...}`，其内容是用户对该段的评论/建议；修改时必须落实这条建议并删除 `\ljz{...}` 本身，不能把批注原样带入论文正文。每次修改原文后，必须在会话中明确告诉用户修改了哪个 section 的哪一段。
- **论文语言风格**：正文倾向使用有逻辑递进的长句/复合句/从句，把因果、限定、转折和结论压缩在同一条清晰论证链中；不建议把相关性很强的论点拆成多句散句，也不建议使用彼此分立、缺少承接关系的短句堆叠。优先写成 concise but logically layered prose，例如 “For scalar Burgers shocks, ..., whereas more fundamentally ...” 这类有层次的句子。
- **并列论点处理**：如果内容本身是缺少强句内递进关系的分立论点，不要硬揉成长句；改用 `itemize` 让结构一目了然，并压缩间距，例如 `\begin{itemize}[leftmargin=*, itemsep=2pt]`。

---

## 防过度AI化

- 必要处加入**motivation**
- **避免 AI 味连接词/破折号/冒号**：除非是十分关键的对比，否则论文正文不推荐使用 `rather than`；也不推荐使用破折号和冒号作为常规连接方式。若在论文撰写或修改中新增、保留或无法避免使用 `rather than`、破折号、冒号，必须在会话中显式报告使用位置：哪个 section、哪一段、哪一句。
- **避免过度适用item/bullet**：这会让文章像是AI写作

### MEMORY 短期迁移记忆规则（强制）

- 当修改某一 section 时，如果判断某段内容/某个论点不适合留在当前位置，但应迁移、展开或延后到后续某个 section，必须在 `lxq/.claude/MEMORY.md` 新增一条**短期迁移记忆**。
- 该短期记忆必须记录：当前 section、待迁移的原文或原意、建议迁移到的目标 section、迁移原因、后续处理触发点。
- 这类记录只服务于后续写作衔接，不是长期项目共识；当后续 section 已经完成迁移、改写、吸收，或确认不再采用该内容时，必须从 `MEMORY.md` 删除该短期记忆。
- 不要把短期迁移记忆混入长期决策日志；建议在 `MEMORY.md` 中单独维护 `Short-term relocation notes` / `短期迁移记忆` 小节，并保持条目少而具体。

### LaTeX 编译工作流（重要）

- **本地无 LaTeX 编译器**（用户已确认）。AI **不要**在本地执行 `latexmk` / `pdflatex` / `xelatex`，会失败。
- AI 写 `.tex` 时确保**语法可解析**（与官方 `neurips_2026.sty` 兼容），不依赖本地编译验证。



